# MASTER CODING PROMPT
## Information Retrieval Coursework — Task 1 + Task 2

Act as a **senior Python full-stack developer, Information Retrieval engineer, web-crawling engineer, MongoDB engineer, and machine-learning engineer**.

Build both coursework tasks as a **complete, executable, production-quality web application**.

The implementation must contain **all backend, frontend, database, crawling, scheduling, Information Retrieval, machine-learning, APIs, testing, and configuration code** required below.

Do not create a superficial prototype.

Do not use fake data where real data is required.

Do not hard-code results.

Every ranking score, clustering result, prediction, and database record must be generated by the actual implementation.

---

# PROJECT STRUCTURE

Create one project containing both tasks:

```text
IR_COURSEWORK/
│
├── README.md
├── .gitignore
├── .env.example
│
├── task1_vertical_search/
│   │
│   ├── backend/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── config.py
│   │   │   ├── routes/
│   │   │   ├── services/
│   │   │   ├── models/
│   │   │   └── utils/
│   │   │
│   │   ├── crawler/
│   │   ├── database/
│   │   ├── ranking/
│   │   ├── scheduler/
│   │   ├── tests/
│   │   ├── requirements.txt
│   │   └── run.py
│   │
│   ├── frontend/
│   │   ├── src/
│   │   │   ├── components/
│   │   │   ├── pages/
│   │   │   ├── services/
│   │   │   ├── hooks/
│   │   │   ├── types/
│   │   │   └── styles/
│   │   ├── package.json
│   │   └── ...
│   │
│   └── README.md
│
├── task2_document_clustering/
│   │
│   ├── backend/
│   │   ├── app/
│   │   ├── database/
│   │   ├── preprocessing/
│   │   ├── clustering/
│   │   ├── models/
│   │   ├── routes/
│   │   ├── services/
│   │   ├── visualization/
│   │   ├── tests/
│   │   ├── requirements.txt
│   │   └── run.py
│   │
│   ├── frontend/
│   │   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   ├── types/
│   │   └── styles/
│   │
│   ├── dataset/
│   ├── scripts/
│   └── README.md
│
└── README.md
```

You may improve the folder structure if necessary, but **backend and frontend for both tasks are mandatory**.

---

# TECHNOLOGY STACK

Use a practical, maintainable stack.

## Backend

Use:

- Python 3.x
- Flask
- REST APIs
- MongoDB
- PyMongo
- Requests
- BeautifulSoup4
- scikit-learn
- NumPy
- pandas
- APScheduler
- python-dotenv
- appropriate logging utilities

## Frontend

Use:

- React
- TypeScript
- Vite
- Tailwind CSS or equivalent
- Axios/fetch for APIs

The frontend must communicate with the Flask backend through REST APIs.

---

# MONGODB

Use MongoDB as the database.

Do not use SQLite as a replacement.

Use environment variables:

```text
MONGODB_URI=
DATABASE_NAME=
```

Never hard-code database credentials.

---

# ============================================================
# TASK 1 — VERTICAL SEARCH ENGINE
# ============================================================

# 1. SEED URL

The crawler MUST start from exactly:

```text
https://pureportal.coventry.ac.uk/en/organisations/centre-for-healthcare-and-community-transformation/
```

This is the vertical-search domain.

Do not replace it with another website.

---

# 2. CRAWLING SCOPE

The crawler must focus ONLY on:

```text
Research Output
Profiles
```

The crawler must:

1. Start at the seed URL.
2. Identify Research Output links.
3. Identify Profile links.
4. Follow relevant pagination.
5. Visit individual Research Output pages.
6. Visit individual Profile pages.
7. Extract required metadata.
8. Store the extracted information in MongoDB.
9. Prevent duplicate URLs/documents.
10. Handle broken pages.
11. Handle network failures.
12. Log crawling activity.

Do not unnecessarily crawl unrelated Coventry University sections.

Do not crawl the entire Coventry University website.

---

# 3. CRAWLER DESIGN

Implement a modular crawler.

Suggested:

```text
crawler/
├── __init__.py
├── crawler.py
├── link_extractor.py
├── research_output_parser.py
├── profile_parser.py
├── url_utils.py
├── crawler_logger.py
└── exceptions.py
```

Create separate functions/classes for:

```text
fetch_page()
extract_research_output_links()
extract_profile_links()
extract_pagination_links()
parse_research_output()
parse_profile()
normalise_url()
is_duplicate()
crawl()
```

---

# 4. CRAWLING PAGINATION

The Coventry Pure portal may paginate research outputs and profiles.

The crawler must detect and follow relevant pagination links.

For example:

```text
?page=1
?page=2
?page=3
...
```

Do not assume only the first page contains data.

The crawler must continue until relevant Research Output/Profile pages have been exhausted.

---

# 5. RESEARCH OUTPUT EXTRACTION

For each individual research output, extract as many relevant fields as available.

Minimum:

```text
title
authors
author profile URLs
published date
research output URL
description/abstract
searchable content
source URL
crawl timestamp
```

Additional metadata may include:

```text
publication type
journal
book
conference
DOI
ISBN
publisher
volume
issue
pages
keywords
```

Do not invent fields that are unavailable.

Store only actual extracted values.

---

# 6. PROFILE EXTRACTION

For each relevant Coventry academic profile, extract:

```text
name
profile URL
academic role
department/centre
research interests
profile description
related research outputs
```

Store the profile URL so that it can be opened from the search results.

---

# 7. MONGODB DATABASE

Create appropriate MongoDB collections.

Minimum:

```text
research_outputs
profiles
crawl_logs
```

Example research document:

```text
{
    "_id": ObjectId,
    "title": "...",
    "authors": [
        {
            "name": "...",
            "profile_url": "..."
        }
    ],
    "publication_date": "...",
    "publication_type": "...",
    "description": "...",
    "content": "...",
    "document_url": "...",
    "source_url": "...",
    "crawl_timestamp": "..."
}
```

Example profile:

```text
{
    "_id": ObjectId,
    "name": "...",
    "profile_url": "...",
    "role": "...",
    "department": "...",
    "research_interests": "...",
    "description": "...",
    "crawl_timestamp": "..."
}
```

Use unique indexes where appropriate.

Prevent duplicate research outputs and profiles.

Use upsert/update behaviour where appropriate so that repeated crawls do not create duplicates.

---

# 8. THREE-MONTH AUTOMATIC CRAWLING

This is a critical requirement.

The crawler must automatically execute:

```text
EVERY 3 MONTHS
```

It must NOT run:

```text
weekly
daily
every 7 days
monthly
```

Use APScheduler or another suitable scheduling mechanism.

Configuration:

```text
CRAWL_INTERVAL_MONTHS=3
```

Implement an actual scheduled job.

Example conceptual flow:

```text
Application starts
      ↓
Scheduler starts
      ↓
Three-month interval
      ↓
Crawler executes
      ↓
Research Outputs + Profiles extracted
      ↓
MongoDB updated
```

The scheduler must not be merely a placeholder.

---

# 9. MANUAL CRAWL ENDPOINT

Also provide a protected/admin development endpoint that can manually trigger the crawler without waiting three months.

For example:

```text
POST /api/crawler/run
```

This is necessary for development/testing.

The automatic scheduler must still remain configured for three months.

---

# 10. CRAWL LOGGING

Store crawl information in MongoDB.

For each crawl:

```text
crawl_id
started_at
finished_at
duration
pages_visited
research_outputs_found
profiles_found
documents_inserted
documents_updated
duplicates_skipped
errors
status
```

Example:

```text
{
    "status": "completed",
    "pages_visited": 120,
    "research_outputs_found": 70,
    "profiles_found": 119
}
```

Values must come from actual execution.

---

# 11. VECTOR SPACE MODEL

The search engine MUST use the:

```text
Vector Space Model
```

Implement the following pipeline:

```text
Research documents
       ↓
Text preprocessing
       ↓
TF-IDF
       ↓
Document vectors
       ↓
User query
       ↓
Query preprocessing
       ↓
Query TF-IDF vector
       ↓
Cosine similarity
       ↓
Ranking
```

Do not replace this with embeddings or an LLM.

---

# 12. TEXT PREPROCESSING

Implement appropriate preprocessing:

```text
lowercase
↓
remove unnecessary punctuation
↓
tokenisation
↓
stop-word removal
↓
optional stemming/lemmatisation
```

Use the same preprocessing logic for:

- indexed documents
- search queries.

Avoid preprocessing that destroys useful names or metadata.

Author names must remain searchable.

---

# 13. TF-IDF

Use a TF-IDF vectoriser.

The system should maintain a fitted vectoriser based on the indexed documents.

For example:

```python
TfidfVectorizer(...)
```

The resulting document matrix must represent the research-output corpus.

---

# 14. COSINE SIMILARITY

Calculate:

```text
cosine_similarity(query_vector, document_vectors)
```

Use the actual values to rank results.

Do not hard-code similarity values.

---

# 15. SEARCH QUERY

The search API must support:

```text
GET /api/search?q=<query>
```

The query may contain:

- research book names
- research output titles
- author names
- academic names
- keywords
- phrases.

Examples:

```text
Machine Learning
Artificial Intelligence
Health Transformation
John Smith
```

---

# 16. AUTHOR SEARCH

Author names must be included in the searchable representation.

Searching:

```text
Adeniyi Fagbamigbe
```

must be able to return relevant research outputs associated with that author.

---

# 17. TOP K = 10

The search engine must use:

```text
k = 10
```

The first page must contain no more than 10 results.

Results must be sorted by:

```text
cosine similarity DESC
```

Example:

```text
1. Research A — 0.9212
2. Research B — 0.8741
3. Research C — 0.8234
```

---

# 18. PAGINATION

If more than 10 relevant documents exist:

```text
Page 1 = results 1–10
Page 2 = results 11–20
Page 3 = results 21–30
```

The backend must support:

```text
page
limit
```

Example:

```text
GET /api/search?q=health&page=2&limit=10
```

The maximum permitted limit should remain 10 for the coursework search interface.

Return metadata such as:

```text
{
    "results": [...],
    "page": 1,
    "limit": 10,
    "total_results": 47,
    "total_pages": 5
}
```

---

# 19. SEARCH RESULT DATA

Each result MUST contain:

```text
title
authors
published_date
document_url
author_profile_urls
cosine_similarity
```

Example:

```text
{
    "title": "...",
    "authors": [...],
    "published_date": "...",
    "document_url": "...",
    "cosine_similarity": 0.8241
}
```

---

# 20. CLICKABLE RESEARCH TITLE

The title must be clickable.

Clicking it must navigate directly to:

```text
the corresponding individual Coventry research-output page
```

Do not navigate to the search page or generic organisation page.

---

# 21. CLICKABLE COVENTRY PROFESSOR/ACADEMIC PROFILE

If a Coventry academic has a profile URL:

```text
author name → clickable
```

Clicking the name must navigate directly to:

```text
the individual Coventry academic profile
```

If a profile URL is unavailable, display the author name as normal text.

---

# 22. TASK 1 FRONTEND

Build a professional web search interface.

Required:

```text
Header
Search bar
Search button
Search statistics
Result cards
Title links
Author links
Publication date
Cosine similarity
Pagination
Loading state
Empty state
Error state
```

Include:

```text
Search results for: "..."
47 results
```

and:

```text
Cosine Similarity: 0.8241
```

Make the interface responsive.

---

# 23. TASK 1 API

Implement suitable APIs:

```text
GET  /api/search
GET  /api/research-output/<id>
GET  /api/profile/<id>
GET  /api/crawler/status
POST /api/crawler/run
GET  /api/crawler/logs
```

Add appropriate validation and error handling.

---

# 24. TASK 1 INDEX MANAGEMENT

Do not recalculate TF-IDF from scratch unnecessarily for every search.

Create an index service that:

1. Loads research outputs.
2. Builds searchable text.
3. Fits TF-IDF.
4. Stores the vectoriser/index in memory or an appropriate persisted form.
5. Rebuilds the index after a crawl when documents change.

The search API should reuse the index.

---

# 25. TASK 1 TESTS

Write automated tests for:

```text
crawler
URL filtering
Research Output extraction
Profile extraction
duplicate prevention
MongoDB insertion
scheduler configuration
TF-IDF
cosine similarity
search
author search
title search
top-k
pagination
API
invalid query
empty query
broken document
```

---

# ============================================================
# TASK 2 — DOCUMENT CLUSTERING
# ============================================================

# 26. TASK 2 OBJECTIVE

Build a complete document clustering and classification application for:

```text
Economics
Entertainment
Politics
```

Use:

```text
K-Means
```

---

# 27. DATASET REQUIREMENT

Minimum:

```text
Economics      >= 150 documents
Entertainment  >= 150 documents
Politics       >= 150 documents
```

Total:

```text
>= 450 documents
```

Prefer long, information-rich documents.

---

# 28. DATA SOURCE

Use one of:

1. Legitimate news scraping
2. A suitable existing dataset
3. A clearly documented self-created dataset

If scraping is selected:

- respect robots.txt
- use reasonable request delays
- identify source
- store source URLs
- avoid excessive traffic.

Do not fabricate articles.

---

# 29. DATASET ORGANISATION

Store:

```text
task2_document_clustering/dataset/
```

Possible structure:

```text
dataset/
├── economics/
├── entertainment/
└── politics/
```

or:

```text
documents.csv
```

with:

```text
id
title
content
category
source
source_url
date
```

---

# 30. DATASET COLLECTION SCRIPT

Create a script such as:

```text
scripts/collect_dataset.py
```

It must:

1. collect documents
2. clean them
3. validate content
4. assign category
5. remove duplicates
6. save dataset
7. print category statistics.

Example output:

```text
Economics: 153
Entertainment: 151
Politics: 156
Total: 460
```

These must be actual counts.

---

# 31. DATASET VALIDATION

Before training, enforce:

```text
economics >= 150
entertainment >= 150
politics >= 150
```

If a category has fewer than 150 documents, the training pipeline should fail with a clear message instead of silently proceeding.

---

# 32. TEXT PREPROCESSING

Implement:

```text
raw document
↓
HTML removal if necessary
↓
lowercase
↓
punctuation cleaning
↓
tokenisation
↓
stop-word removal
↓
optional lemmatisation/stemming
↓
clean text
```

Do not remove meaningful terms unnecessarily.

---

# 33. TF-IDF

Convert documents into TF-IDF vectors.

Use the same vectoriser later for user input.

Do not fit a new vectoriser independently for every prediction.

---

# 34. K-MEANS

Use:

```text
K = 3
```

Train:

```python
KMeans(n_clusters=3, ...)
```

Use appropriate:

- random_state
- n_init
- max_iter

for reproducibility and convergence.

---

# 35. CLUSTER LABEL MAPPING

K-Means cluster IDs are arbitrary.

Determine the actual mapping:

```text
Cluster X → Economics
Cluster Y → Entertainment
Cluster Z → Politics
```

based on the known labelled training dataset.

Do not arbitrarily assume:

```text
Cluster 0 = Economics
Cluster 1 = Entertainment
Cluster 2 = Politics
```

unless the actual data demonstrates that mapping.

---

# 36. SAVE TRAINED MODEL

Persist the trained:

```text
TF-IDF vectoriser
K-Means model
cluster-to-category mapping
```

Use an appropriate model persistence method such as:

```text
joblib
```

Suggested:

```text
models/
├── tfidf_vectorizer.joblib
├── kmeans_model.joblib
└── cluster_mapping.json
```

The prediction API should load these models rather than retraining every request.

---

# 37. USER CLASSIFICATION

The frontend must allow input:

```text
sentence
statement
paragraph
document
```

Example:

```text
The central bank increased interest rates to control inflation.
```

The system must output:

```text
Economics
```

The actual output must be produced by the K-Means model.

---

# 38. PREDICTION PIPELINE

Implement:

```text
User input
      ↓
Preprocessing
      ↓
Saved TF-IDF vectoriser
      ↓
TF-IDF vector
      ↓
K-Means nearest centroid
      ↓
Cluster ID
      ↓
Cluster-to-category mapping
      ↓
Prediction
```

Do not retrain K-Means during prediction.

---

# 39. PREDICTION INFORMATION

Return:

```text
input_text
predicted_category
cluster_id
distance_to_centroid
timestamp
```

If a confidence score is displayed, ensure it is mathematically justified.

Do not call an arbitrary distance a probability.

---

# 40. MONGODB PREDICTION STORAGE

Store every user classification in MongoDB.

Collection:

```text
classification_history
```

Example:

```text
{
    "_id": ObjectId,
    "input_text": "...",
    "predicted_category": "Economics",
    "cluster_id": 1,
    "distance_to_centroid": 0.842,
    "timestamp": "..."
}
```

---

# 41. TASK 2 API

Implement:

```text
POST /api/classify
GET  /api/classification-history
GET  /api/clustering/statistics
GET  /api/clustering/visualization
```

Example request:

```json
{
    "text": "The government announced a new economic policy."
}
```

Example response:

```json
{
    "prediction": "Economics",
    "cluster_id": 0,
    "distance_to_centroid": 0.72
}
```

---

# 42. TASK 2 FRONTEND

Create a professional classification interface.

Include:

```text
Document input
Character/word count
Classify button
Clear button
Prediction card
Category
Cluster ID
Distance
Classification history
```

Make the predicted category visually prominent.

---

# 43. CLUSTERING VISUALISATION

Generate a graph using:

```text
PCA
```

or:

```text
TruncatedSVD
```

to reduce the TF-IDF matrix to two dimensions.

The actual clustering remains K-Means.

The graph must show:

```text
Cluster 0
Cluster 1
Cluster 2
```

and/or their mapped category names.

Generate and save the graph.

Example:

```text
visualization/
└── kmeans_clusters.png
```

---

# 44. CLUSTERING METRICS

Calculate actual:

```text
inertia
silhouette_score
```

where mathematically appropriate.

If labelled evaluation is performed after mapping clusters:

```text
accuracy
precision
recall
F1
confusion matrix
```

may also be calculated.

Never fabricate metrics.

---

# 45. TASK 2 MODEL TRAINING SCRIPT

Create something like:

```text
scripts/train_model.py
```

It must:

1. load dataset
2. validate categories
3. preprocess
4. create TF-IDF
5. train K-Means
6. determine cluster mapping
7. calculate metrics
8. save models
9. generate visualisation
10. print training summary.

---

# 46. TASK 2 TESTS

Write automated tests for:

```text
dataset validation
preprocessing
TF-IDF
K-Means K=3
model loading
cluster mapping
Economics prediction
Entertainment prediction
Politics prediction
empty input
long input
invalid input
MongoDB storage
API
```

---

# ============================================================
# SHARED QUALITY REQUIREMENTS
# ============================================================

# 47. ERROR HANDLING

Both applications must handle:

```text
network errors
database errors
invalid input
empty input
missing documents
404
500
timeouts
malformed data
duplicate records
```

Return meaningful API responses.

---

# 48. LOGGING

Implement Python logging.

Log:

```text
crawler start
crawler completion
database errors
scheduler execution
index rebuild
model training
classification
API errors
```

Do not log passwords or secrets.

---

# 49. SECURITY

Implement basic security:

- environment variables
- input validation
- no exposed credentials
- no hard-coded MongoDB passwords
- safe API handling
- reasonable request validation.

---

# 50. CORS

Configure CORS appropriately for frontend/backend development.

Do not use unnecessarily broad production permissions if avoidable.

---

# 51. RESPONSIVE FRONTEND

Both task interfaces must work on:

- desktop
- tablet
- mobile.

Use accessible labels and semantic HTML.

---

# 52. API ERROR FORMAT

Use a consistent API error structure.

Example:

```json
{
    "success": false,
    "error": {
        "code": "INVALID_QUERY",
        "message": "Search query cannot be empty."
    }
}
```

---

# 53. API SUCCESS FORMAT

Use consistent success responses.

Example:

```json
{
    "success": true,
    "data": {}
}
```

---

# 54. ENVIRONMENT CONFIGURATION

Create:

```text
.env.example
```

Example:

```text
MONGODB_URI=
DATABASE_NAME=
CRAWL_INTERVAL_MONTHS=3
BACKEND_PORT=5000
FRONTEND_PORT=5173
```

Never commit real `.env`.

---

# 55. README

Create a README explaining only the implementation:

- prerequisites
- installation
- MongoDB setup
- environment variables
- Task 1 startup
- Task 1 crawler
- Task 1 scheduler
- Task 1 search
- Task 2 dataset
- Task 2 model training
- Task 2 classification
- tests
- API endpoints
- project structure.

---

# 56. ACTUAL EXECUTION

After writing the code, actually execute the project.

Do not stop after generating source files.

Perform:

## Task 1

```text
Install dependencies
↓
Start MongoDB
↓
Run backend
↓
Run crawler
↓
Verify MongoDB
↓
Build TF-IDF index
↓
Perform search
↓
Verify cosine similarity
↓
Verify top 10
↓
Verify pagination
↓
Verify research links
↓
Verify profile links
↓
Verify scheduler configuration
```

## Task 2

```text
Prepare dataset
↓
Verify >=150/category
↓
Preprocess
↓
Train K-Means
↓
Verify K=3
↓
Generate visualisation
↓
Save models
↓
Start backend
↓
Classify test statements
↓
Verify MongoDB
```

---

# 57. FINAL CODE AUDIT

Before finishing, inspect every source file and verify:

### Task 1

```text
✓ Crawler
✓ Research Output filtering
✓ Profile filtering
✓ Pagination crawling
✓ Data extraction
✓ MongoDB
✓ Scheduler = 3 months
✓ TF-IDF
✓ Vector Space Model
✓ Cosine similarity
✓ Author search
✓ Title search
✓ Top-k = 10
✓ Pagination
✓ Research links
✓ Profile links
✓ API
✓ Frontend
✓ Tests
```

### Task 2

```text
✓ Dataset >=450
✓ >=150/category
✓ Preprocessing
✓ TF-IDF
✓ K-Means
✓ K=3
✓ Cluster mapping
✓ Model persistence
✓ User classification
✓ Economics
✓ Entertainment
✓ Politics
✓ MongoDB
✓ Visualization
✓ API
✓ Frontend
✓ Tests
```

---

# 58. FINAL RULE

Build the actual application.

Do not merely explain how it could be built.

Do not provide pseudocode instead of working code.

Do not replace required algorithms with unrelated techniques.

Do not fabricate data.

Do not fabricate results.

Do not hard-code similarity values.

Do not hard-code classification results.

Do not hard-code cluster assignments without calculating the mapping.

Do not implement the crawler as weekly.

The Task 1 scheduler MUST be:

```text
3 MONTHS
```

The Task 1 ranking MUST use:

```text
Vector Space Model + TF-IDF + Cosine Similarity
```

The Task 1 retrieval MUST use:

```text
K = 10
```

The Task 2 clustering MUST use:

```text
K-Means
K = 3
```

The Task 2 dataset MUST contain at least:

```text
150 Economics
150 Entertainment
150 Politics
```

The final project must be executable from the supplied README and contain **complete backend + frontend code for both tasks**.